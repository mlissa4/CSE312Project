version: '3'
services:
  mongo:
    image: mongo:4.2.5
    ports:
      - "27017:27017"
  app:
    build: .
    environment: 
      WAIT_HOSTS: mongo:27017
      secret_key: replace
      salt: replace
    ports: 
      - "8080:8080"
    
    